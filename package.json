{
  "name": "oziebot",
  "private": true,
  "workspaces": [
    "frontend/apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w web",
    "lint:web": "npm run lint -w web"
  },
  "engines": {
    "node": ">=20"
  }
}
